# Greenter Adapter

Internal PHP Composer project for Librefact's Greenter boundary.

## Local Setup

```bash
composer install --working-dir services/greenter-adapter
```

## Test

```bash
composer test --working-dir services/greenter-adapter
```

The first test proves `Greenter\See` is autoloadable through Composer.
